apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-myapp
  labels:
    app: {{ .Release.Name }}-myapp
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Release.Name }}-myapp
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
